import 'package:flutter/material.dart';

class Neopixels extends StatelessWidget {
  const Neopixels({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Neopixels"),
    );
  }
}